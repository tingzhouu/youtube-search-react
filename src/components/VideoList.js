import React from 'react';

class VideoList extends React.Component {
  render() {
    return (
      <h1>VideoList</h1>

      // let videoListToRender = this.props.videos.map((video) => {
      //   return (
      //     <div>
      //       {video.id.videoID}
      //     </div>
      //   );
      // })
  
      // let videoList = this.props.videos;
      // const newList = videoList.map((video) => {
      //   return <h1>{video.id.videoId}</h1>;
      // });
      // console.log(newList);
    );
  }
}
export default VideoList;